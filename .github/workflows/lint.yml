on:
  workflow_call:

jobs:
  Install:
    uses: ./.github/workflows/install-deps.yml
  Lint:
    needs: Install
    runs-on: ubuntu-latest
    steps:
#      - name: Checkout
#        uses: actions/checkout@v2
#        with:
#          fetch-depth: 0
      - name: Lint
        run: npm run lint